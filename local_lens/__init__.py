"""Local Lens: local-first screenshot and document intelligence core.

This package is intentionally framework-agnostic (no Streamlit imports) so it
can later be driven by a CLI, a FastAPI service, an MCP server, or a desktop
shell in addition to the current Streamlit UI in app.py.
"""
