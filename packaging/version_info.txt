# PyInstaller Windows version-resource file (pyi-grab_version format).
# Embeds basic file/product metadata into LocalLens.exe -- no extra
# tooling or versioning framework, just PyInstaller's own `version=`
# argument to EXE() in local_lens.spec. Version numbers mirror
# pyproject.toml's `version = "0.4.0"` (the whole project's version, not
# a separate desktop-only scheme) plus a trailing build-number slot
# PyInstaller's four-part format requires.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 4, 0, 0),
    prodvers=(0, 4, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          "040904B0",
          [
            StringStruct("CompanyName", "Local Lens"),
            StringStruct("FileDescription", "Local Lens -- local-first screenshot OCR"),
            StringStruct("FileVersion", "0.4.0.0"),
            StringStruct("InternalName", "LocalLens"),
            StringStruct("OriginalFilename", "LocalLens.exe"),
            StringStruct("ProductName", "Local Lens"),
            StringStruct("ProductVersion", "0.4.0.0"),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct("Translation", [1033, 1200])]),
  ],
)
