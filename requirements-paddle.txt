# *** LEGACY / OPTIONAL LOCAL HEAVY BACKEND -- NOT REQUIRED ***
#
# Local Paddle/PaddleOCR-VL installation is optional and resource-intensive.
# It is NOT required for standard Local Lens usage: Fast mode runs on
# EasyOCR alone, and Deep Analyze uses a remote, bring-your-own-endpoint
# provider (see local_lens/deep_analysis/ and README.md "Deep Analyze")
# instead of a local PaddleOCR-VL install.
#
# paddlepaddle + paddleocr + the paddlex[ocr] extra (needed for local table
# extraction and the legacy local PaddleOCR-VL path) can together consume
# several GB of disk and pull a large model cache into ~/.paddlex on first
# use. Do not install this on a disk- or RAM-constrained machine. Suitable
# environments: a cloud/dev box or CI runner with disk to spare, not a
# primary laptop running low on space.
#
# Kept separate from requirements.txt so the default `pip install -r
# requirements.txt` never installs Paddle.
#
# Install with (only if you have deliberately decided you need it):
#   pip install -r requirements.txt
#   pip install -r requirements-paddle.txt
#
# See README.md "PaddleOCR" section for platform notes/known issues.

paddlepaddle>=3.0.0
paddleocr>=3.0.0

# Table extraction (local_lens/tables/) and the PaddleOCR-VL experiment
# (experiments/paddleocr_vl/) both need paddlex's "ocr" extra on top of the
# above -- it pulls in table/layout/document-parsing sub-dependencies
# (scikit-learn, premailer, openpyxl, etc.) that the plain text pipeline
# doesn't need. Install separately since it's a meaningfully larger,
# slower install:
#   pip install "paddlex[ocr]==<paddlex version installed above>"
